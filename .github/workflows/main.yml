name: CI
on: [push]
jobs:
  hasura_cli:
    name: Run Hasura cli
    runs-on: ubuntu-latest
    steps:
    - name: Check hasura cli version
      uses: worklifebeyond/hasura-cli-action@master
      with:
        args: |
          version
